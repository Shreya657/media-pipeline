{
  "name": "media-processor-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=api-server"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  }
}
